// If no access modifier is specified, the class is package-private by default.
// It is accessible only within the same package; it cannot be accessed from outside its package.
class DefaultClass {
    void display() {
        System.out.println("This is a default (package-private) class.");
    }
}